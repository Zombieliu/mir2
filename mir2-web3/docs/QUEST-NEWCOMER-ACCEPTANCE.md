# 新手任务引导验收

2026-09-11 NPC入口实机补充：固定440x224公告板改为NPC名header、9行正文/服务分页、独立QUEST footer和右上关闭按钮。实机在公告板(335,259)验证无原始markup泄漏，Create Hero标签正确，下页可见ReviveHero/SealHero/Use；Use进入服务端商店列表，末页可见Hairdresser/Close。QUEST打开独立5项任务列表，X同时关闭两层。真实Board parser 1/1、启用`native-ui`的UI测试1/1（805 filtered）及两端构建通过；相关Native UI回归集4/4包含该分页用例，不能重复计数。首次0-test运行不计。本轮只调QA位置，没有改任务进度。

这是一轮入口可用性修复。Crystal原位内联、彩色大按钮布局和重复fallback footer仍待处理；Hero各项业务动作没有逐一实机验证。`formalCandidate=false`、`accepted=false`、`visualAccepted=false`。

2026-09-11，用户授权在保留Crystal对照的同时优化前15级体验。
实现配置：`config/quest-guidance/newcomer-v1.json`。

## 自动验证记录

- 当前源码Rust 1.95.0，client-bevy `--features native-ui --lib quest_`：87通过，0失败。
  覆盖原任务回归及新增排序、详情换行、固定零奖励过滤和原选择编号的Finish校验。
- Node任务路线/策略及新手配置：192通过，0失败；其中新增配置验证5项。
- 三职业优化路线生成成功，原版文件名和原始任务字段保留。
- 只读复核没有阻断问题；Rust格式检查与diff检查通过。
- 日志：`C:/mir2-three-class-r4-20260910-evidence/quest-newcomer-rust.log`、
  `C:/mir2-three-class-r4-20260910-evidence/quest-newcomer-node.log`。

本轮未重打Windows包、未启动游戏或服务端；以下视觉和成长验收仍未完成。

## 范围

每职业42项：22推荐、15支线、4挑战、1低优先级；三职业并集48项。
推荐路线保留每一个前置，戒指递送29→30→31→32及跑腿43→44→45整体归支线。
152→153完整归挑战；154为空目标零奖励的跨图条目，降低推荐优先级。

本轮属于可选展示优化，不计入Crystal原版UI一致性百分比。
没有改变经验曲线、怪物、掉率、等级限制或已保存的任务状态。

## Windows 对照步骤（尚待实机）

使用同一新构建，在 `MIR2_QUEST_GUIDANCE=crystal` 与 `newcomer-v1` 间重启切换：

1. 在包含多项任务的NPC处，用鼠标和键盘分别选择任务，确认名称、详情、提交ID一致。
2. 同时有推荐任务和可交付支线时，确认可交付任务排在前面。
3. 日记按四类显示当前任务；未知或15级以外任务仍可见，没有误删任务。
4. NPC详情与日记详情均显示引导文字；检查换行、滚动、遮挡和100/125/150%缩放。
5. 战法道分别检查导师后续提示：Fencing被动、FireBall目标施法、Healing治疗。
6. q37/q41固定0数量奖励不显示成可得装备；实际提交的金、经验、背包变化与服务端一致。
7. 可选奖励的选择编号不随隐藏固定奖励改变；只有0数量选择项时仍能选择和交付。
8. 退出重登后任务进度和手动追踪保留；切回Crystal模式恢复原分组与顺序。

## 正常成长记录（尚待实机）

每职业使用独立普通新角色，不加等级、技能或物品，不用QA传送绕过路线。
按1–4、4–7、7–10、10–15阶段记录：起止等级/经验、耗时、任务ID、
药水消耗、金币净变化、死亡、往返地图次数、背包满次数及已学可用技能。
中断和补刷经验也应记录，不能用修改角色等级补齐证据。

根据这些数据再决定经验、收集数量、奖励装备或挑战解锁等级的调整。
本配置中的推荐/挑战属于设计判断，不是三职业难度已经平衡的结论。
