# 收据作用域与主备重放复审（2026-09-09）

审查范围限于本轮新增的 Zone 收据命名空间、Direct 请求计数和 Hosted 重放上下文。代码版本：`apps/gateway/src/routing.rs` SHA256 `eaa7e33b60baecfc1827b2053f2d243a922e01498579d08c850b0c139302c64a`。

首轮发现一个 P1：active 使用日志序号，备用重放却丢失上下文而递增另一个持久计数器，会导致状态分叉。修正后两边都保留已验证的日志排序信息；Direct、未认证命令和备用重放的 `external_commit_authorized` 为 false。实际经济服务各入口继续校验该标志，`commit_in_zone` 默认委托原有 `commit_fenced`，没有把命名空间作为外部提交授权。

内存收据采用实际 factory ZoneId、排序 / 本地序号域和原命令身份的 JSON 元组，避免跨区复用旧回执和分隔符混淆；不包含 fencing epoch，允许同一逻辑命令在换主后去重。本地序号来自已有共享 checkpoint 计数器，运行时重建不再从头计数。

修正后独立只读复审未发现本次差异中的额外 P1/P2。验证：收据专项 4/4、掉落回归 47/47、经济及真实 Hosted 主备重放 39/39、最终源码 RPC 30/30、Gate11 2/2。此记录不把已有缓存架构、全游戏经济一致性或生产冷地图启动性能标记为全面认证。
