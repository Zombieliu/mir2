# Safe profile and ordinary controller repairs

Accepted Crystal-map Walk/Run immediately projects manifest safe-area geometry
into the shared Zone combat profile. Rejected movement, Turn and unknown custom
maps preserve the existing trusted value. Focused Zone tests pass **4/4**,
including real shared player-damage admission on entry and exit. This is not an
actual monster-AI combat test. The public Gateway snapshot reads that same Zone
profile before the personal mirror drains its pending transform. Its isolated
V2 entry/private-Tick/exit integration passes **1/1**.
The existing public arrival/direct/pending/autosave/rejection regressions pass
**4/4** in this same fresh Gateway harness (`420d7f49976063ae`).

V2 purchases one required HP potion from the live Samuel actor at (324,291),
using his real BuySell goods row and requiring fresh exact gold and inventory
receipts. V1 village restocking is unchanged. V2 also refreshes the public
snapshot while waiting for skill cooldowns. No server cooldown, movement speed,
level curve or live save records were changed for these controller repairs.

The V2-only stale-corpse retry requires a fresh received dead-target snapshot
after engagement and no accepted owner attack/cast or known strike on that
target. It awards no credit; all completion still requires the authoritative
quest projection. The existing engagement and absolute-time budgets remain.
A known strike from any attacker, including a possible owner pet, preserves
the missing-award failure. V1 keeps its strict default.

Controller regression results: combat **150/150**, supplies **52/52**, V2
integration **30/30**, combined **232/232**. Independent bounded source review
found no blockers. Raw evidence is retained in this directory.

The deployed N5 release remains source `83c157b46` with SHA256
`FD658D1881417FA4B5B42C6A0838C3891E010342FB6888722B7E50E9BEF961D3`.
`build-safe-arrival.json` explicitly says `zoneProfileFixIncluded=false`:
the new safe-profile Rust repair is tested source, not part of that running EXE.
The three ordinary characters resumed with the new controller source and their
original deadlines. Later saved checkpoints are recorded separately.

No native computer-control service was available. Native UI/animation, original
Crystal comparison, clean two-hour timing and the complete level-30 journey
remain open. `visualAccepted=false`, `measuredTime=false`,
`globalParityPercent=null`.
