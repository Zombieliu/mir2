# Compose contract audit — 2026-09-21

Source: E:/mir2/Crystal/Client/MirScenes/Dialogs/MailDialogs.cs, MirControls/MirItemCell.cs and Server/MirObjects/PlayerObject.cs. This is an implementation audit, not visual acceptance.

Original letter: Title671 236x300 at(100,100), recipient(70,35), multiline body(15,92,202,165), SendTitle607-60976x25 at(30,265), CancelTitle193-19568x25 at(135,265). List Send first opens centered modal MirInputBox (Prguse660288x156); Reply prefills sender. Parcel is separate Title674236x384 at(inventory width+10,0), body y98, stamp Prguse2203/20420x18 at(73,56), quoted postage(63,269), gifted gold(63,290), five35x31 cells at(27+36i,311), Send/Cancel y350. Without a real stamp only first cell is enabled; Title676 covers remaining cells. Seven missing frames are now exported and source-verified; export does not mean the controls are integrated.

Current native compose remains the generic combined form. Confirmed missing: separate letter/parcel pages and recipient modal, actual textarea/caret/selection behavior, MailCost request/result and correlated current-draft quote, owned stamp selection plus authoritative consumption, original one/five-slot rules, draft attachment locks/unlocks, source drag/drop and gold amount prompt. Do not label it Crystal-complete or remove existing parcel functionality merely to make a smaller letter-only acceptance pass.

Candidate service revalidates authenticated durable sender, trimmed/control-free recipient up to20 scalars, existing recipient, bounded exact attachment IDs, gold and durable item identity. It atomically commits delivery/debit/removal. Native previously typed32 recipient scalars,256 body scalars and serialized stamped=false. Current five-item adapter postage floor(gold/1000)*100 differs from configurable Crystal item insurance/stamp rules. Native quote/stamp fields alone must not bypass authoritative validation.

This round repairs real input defects: message Enter/NumpadEnter preserves LF without submitting; numeric gold input and backspace now edit only the draft with checked overflow; recipient input matches20-scalar server limit. Simulation allows CR/LF in bounded mail bodies while preserving rejection of all other controls and existing transaction rules. A temporary-account ordinary SendMail test verifies body persistence after reload; it does not send to real users. Native typing integration977/977 and UI-core43/43 pass. Full backend results and package binding are recorded in README.md.
Additional entry-path evidence: Crystal GameScene.cs MailSendRequest opens the recipient MirInputBox, then MailComposeParcelDialog and InventoryDialog. Source NPCScript.cs1079 emits this packet. Candidate protocol233 and gateway web.rs serialization exist, but repository search finds no Simulation emission or native runtime handling of MailSendRequest. Thus NPC-to-parcel entry also needs implementation; merely skinning the existing generic compose form does not restore the source flow. Separately, MailComposeUi.last_notice currently has no renderer; visible failure feedback is being corrected before further compose parity.