# Compose contract audit — 2026-09-21

Source: E:/mir2/Crystal/Client/MirScenes/Dialogs/MailDialogs.cs, MirControls/MirItemCell.cs and Server/MirObjects/PlayerObject.cs. This is an implementation audit, not visual acceptance.

Original letter: Title671 236x300 at(100,100), recipient(70,35), multiline body(15,92,202,165), SendTitle607-60976x25 at(30,265), CancelTitle193-19568x25 at(135,265). List Send first opens centered modal MirInputBox (Prguse660288x156); Reply prefills sender. Parcel is separate Title674236x384 at(inventory width+10,0), body y98, stamp Prguse2203/20420x18 at(73,56), quoted postage(63,269), gifted gold(63,290), five35x31 cells at(27+36i,311), Send/Cancel y350. Without a real stamp only first cell is enabled; Title676 covers remaining cells. Seven missing frames are now exported and source-verified; export does not mean the controls are integrated.

Current candidate has a source-sized independent Letter page and recipient modal, with isolated Letter/Parcel drafts and guarded external entry paths. Parcel remains the generic gift/attachment form. Confirmed missing: complete source Parcel page and its movable frame, Letter clipboard/IME and explicit wheel scrolling, MailCost request/result and correlated current-draft quote, owned stamp selection plus authoritative consumption, original one/five-slot rules, draft attachment locks/unlocks, source drag/drop and gold amount prompt. Do not label it Crystal-complete or remove existing parcel functionality merely to make a smaller letter-only acceptance pass.

Candidate service revalidates authenticated durable sender, trimmed/control-free recipient up to20 scalars, existing recipient, bounded exact attachment IDs, gold and durable item identity. It atomically commits delivery/debit/removal. Native previously typed32 recipient scalars,256 body scalars and serialized stamped=false. Current five-item adapter postage floor(gold/1000)*100 differs from configurable Crystal item insurance/stamp rules. Native quote/stamp fields alone must not bypass authoritative validation.

This round repairs real input defects: message Enter/NumpadEnter preserves LF without submitting; numeric gold input and backspace now edit only the draft with checked overflow; recipient input matches20-scalar server limit. Simulation allows CR/LF in bounded mail bodies while preserving rejection of all other controls and existing transaction rules. A temporary-account ordinary SendMail test verifies body persistence after reload; it does not send to real users. Native typing integration977/977 and UI-core43/43 pass. Full backend results and package binding are recorded in README.md.
Additional entry-path evidence: Crystal GameScene.cs MailSendRequest opens the recipient MirInputBox, then MailComposeParcelDialog and InventoryDialog. Source NPCScript.cs1079 emits this packet. Candidate protocol233 and gateway web.rs serialization exist, but repository search finds no Simulation emission or native runtime handling of MailSendRequest. Thus NPC-to-parcel entry also needs implementation; merely skinning the existing generic compose form does not restore the source flow. Visible validation/operation failures now use the source message frame (980-test feedback checkpoint). Sending/in-flight status and original compose surface remain distinct follow-up work.

## Letter editing and movable frame candidate

Letter now uses shaped glyph positions for its caret, pointer selection, vertical navigation and automatic caret scrolling, with Unicode grapheme-aware deletion. The source-sized window can be dragged; higher modal layers, focus loss and session reset cancel captures. A retained draft keeps its selection through frame moves and temporary modal coverage. Pending sends freeze edits. Shared wrap-boundary caret affinity and the 198x161 content viewport have dedicated regressions.

Native UI library: 994 passed, 0 failed (C:/mir2-ui-repair-20260921/mail-editor-final-tests.log). These are code tests, not desktop visual acceptance. Clipboard, IME, explicit wheel scrolling, complete original Parcel/service flow, and same-build native visual checks remain open. Existing 256-unit editor limit is not claimed to match the original server limit.

## Parcel backend checkpoint (not deployed)

The earlier missing Simulation emission is now repaired: authenticated active NPC dialogs with an offered @SendParcel link and valid interaction range emit MailSendRequest. Postage includes source per-item insurance; an owned Type0/Shape1 stamp is validated and consumed within the existing atomic sender/recipient transaction. Without an owned stamp only the first parcel slot is allowed. Invalid quotes return the protocol's maximum cost sentinel; duplicate identities, NoMail and changed durable item/stamp states reject delivery. Persistence failure preserves currency, attachments and stamp. Real player accounts were not modified by these fixture tests.

Verified logs under C:/mir2-ui-repair-20260921/: mail-parcel-regression-rerun.log (52/52), parcel-route-final-tests.log (12/12), mail-stale-save-tests.log (1/1), mail-stale-shop-tests.log (1/1). Filters overlap and are not a unique-test total. Frontend Parcel and transport edits are still uncompiled at this checkpoint. No new package, live delivery, visual or source-window acceptance is claimed; the user's settings-guidance client and existing Gateway remain running unchanged.

## Parcel frontend integration follow-up

The independent source Parcel frame, recipient prompt, inventory attachment drag, gold prompt and owned-stamp toggle now compile. Quote state retains the one outstanding request across timeout and local close, consumes its eventual response against the current draft, and never blindly retries an uncorrelated request. Quote fingerprints include live item pricing inputs so same-UID changes invalidate a stale price. Shared drag/button systems no longer require Parcel resources in unrelated storage test harnesses. Full native UI library: 1016 passed / 0 failed (C:/mir2-ui-repair-20260921/parcel-ui-pass-tests.log). This supersedes the intermediate 40-, 9-, and 3-failure runs, which remain available for diagnosis.

Still open: native runtime/Windows integration checks and package, ordinary native send/cancel/claim visual evidence, clipboard/IME/wheel parity, selected-item lock glyph, and transport-overflow recovery. Read-only review found that a saturated native/service inbox can discard the sole Cost reply; the safe single-flight timeout then remains blocked until session reset. Normal timeout must not issue another request to mask this issue because late replies have no request identifier. No completion or visual acceptance follows from the library result.

## Bounded postage-response preservation (2026-09-22, not deployed)

Native inbound and UI service queues now each retain one typed Cost(u32) overflow slot without replacing their original FIFO contents or the GameShop receipt reserve. Later service events cannot overtake a reserved Cost. Native forced eviction for ACK/GameShop also skips an already-enqueued valid Cost; a duplicate queued Cost is rejected because the UI has only one request in flight. DataReset and runtime destruction clear old reserves; reset-barrier tests preserve a new-session response admitted after that reset. UI inbox nonempty/length/pop semantics include its reserve.

Invalid quote sentinel u32::MAX is shown as unavailable, not4294967295 gold, and cannot enable Send. It remains associated with the rejected draft to avoid automatic per-frame retry; changing the draft permits a new quote. Existing timeout still never guesses a replacement request's correlation.

UI library1020/1020 passes (C:/mir2-ui-repair-20260921/mail-cost-feedback-client-tests.log); delegated runtime cargo test250/250 passes, including both forced-eviction cases, saturated queues and reset generations. Earlier248-test runtime evidence is mail-cost-reserve-runtime-tests.log; it precedes the two forced-eviction tests and must not be relabelled250. Windows parcel host tests3/3 pass (mail-cost-guard-windows-tests.log). These close source/test overflow defects only; no ordinary live mailbox operations or visual acceptance were performed.

## Mail body limit alignment — 2026-09-22 (not deployed)

Crystal PlayerObject.cs:11718/11771 rejects bodies exceeding 500 UTF-16 code units. Both native Letter/Parcel editor and fallback input now use the same 500-unit budget; the authenticated backend validation counts UTF-16 rather than allowing1000 Unicode scalars. Existing saved drafts are not migrated or truncated. ASCII500/501, emoji250/251, mixed Chinese/emoji and newline boundaries are tested through both frontend input paths; backend tests retain CR/LF and illegal-control rejection. UI mail regressions71/71 and simulation mail regressions53/53 pass (mail-body-ui-regression.log and mail-body-backend-tests.log under C:/mir2-ui-repair-20260921). No real mail was sent. Clipboard, IME, wheel editing and same-version visual acceptance remain open.

Correction to earlier lock-glyph wording: Crystal MirItemCell.cs:2538 renders locked/selected inventory items DimGray at0.8 opacity; GameScene.cs:6471 applies MailLockedItem to the inventory cell. The missing parity behavior is inventory dimming/input lock, not a separate lock icon. No icon should be invented as an original-parity requirement.

Remaining attachment lock audit: current MailParcelUi.locked_ids is populated/cleared but has no read consumer outside mail_parcel.rs. render_inventory has no Parcel state argument; inventory_item_drag_at_cursor selects any current bag UID. Therefore server echo storage alone does not prove original locked-cell dimming or input suppression. A future fix must bind the exact selected/acknowledged UID to inventory rendering and all item mutation entry paths, and release it on removal/cancel/send/reset without locking a replacement item in the same slot. Do not treat the backend durable identity revalidation as UI parity or describe these temporary client locks as server escrow.

## Mail wheel candidate (source tested, not packaged/deployed)

Letter and Parcel body viewports now accept wheel line/pixel scrolling using captured glyph layout and Crystal stage scaling. Caret/selection are retained; unchanged per-frame layout capture no longer pulls manual scroll back to the caret. Shortened layouts clamp the old scroll extent. Outside-body events, nonfocused windows, modal coverage, dragging and pending sends do not scroll the body. Foreground consumption also prevents the independently rendered chat HUD from scrolling beneath a moved mail window; the chat system already runs after NativePlayerUiSet::Mutate. Consumed events do not replay on the next update.

Full native UI library1025/1025 passes in C:/mir2-ui-repair-20260921/mail-wheel-ui-full-rerun.log. Initial parent chat regression compile failed because MouseWheel required TouchPhase; the fixture was corrected without changing the behavior assertion. Delegated mail-only tests74/74 preceded the added chat guard. Source/test evidence only: no screenshot, clipboard/IME acceptance, live delivery or complete mail-page pass is claimed. The prepared3b8bd01f9 mail-body package predates this wheel patch.

Attachment-lock implementation in progress (not yet test/deployment evidence): original Crystal waits for false MailLockedItem echo after detach/cancel. Current bounded native and UI inboxes can drop that echo, while backend implements no escrow. Candidate therefore releases the exact local UID lock immediately on detach/cancel/invalidated selection and still sends false; stray later true echoes cannot recreate an unselected lock, and later false cannot unlock a newly selected attachment. This deliberately differs from original ACK timing to prevent permanent UI freeze and adds no retry loop or invented server custody. Selected attachments remain blocked before ACK and on failed send; successful send/session reset clear their local state. Test results must be recorded separately after integration.

Attachment-lock source/test checkpoint: exact selected UID now disables carried-cell interactions and tints the actual icon DimGray(105/255), opacity0.8 while preserving transparent padding. Bag action buttons, drag sources and locked bag destinations, stale delete confirmations, and bag-to-belt armed moves recheck the lock. No unrelated item/operation lock is cleared. Late/unknown echoes, cancellation with lost ACK, reselect, success/reset, rendering and action-chain regressions pass. Source stamp checkbox also uses203 for unstamped and204 for stamped across normal/hover/pressed states, matching Crystal UpdateParcel rather than treating these as three interaction sprites.

Full native UI1028/1028 passes (mail-lock-ui-full-tests.log); Windows parcel host3/3 passes (mail-lock-windows-tests.log). Earlier scoped parcel13/13 is included, not additive. Current prepared159cfc832 wheel package does not contain these later edits. No native lock/cancel screenshot or operation acceptance was performed; clipboard/IME and remaining37-page evidence gates remain open.

## Clipboard and IME candidate — source/test only

The real Letter/Parcel editor now has a separate mail text target; it is not routed through FriendDialog. Host copy/cut use the current selection, and asynchronous paste binds compose kind, recipient, draft epoch, editor revision and session revision. Focus/coverage/close/pending-send changes invalidate the target; mismatched current draft/editor contents reject input until synchronization. Clipboard access remains in the existing platform host.

IME Preedit is a separate presentation buffer with its own shaped layout/caret; it does not edit the authoritative draft or emit mail. Commit uses the500 UTF-16 insertion path and marks the input frame consumed. Candidate positioning uses the actual text node transform, including its scroll offset once. Review found the first implementation still used committed-text geometry during Preedit; the corrected separate display layout covers wrapped/scrolling preedit and a committed-layout first-frame fallback. Modifiers reset on composition changes.

Full client UI1035/1035 passes (mail-text-client-full.log); Windows clipboard module13/13 passes (mail-text-clipboard.log). Focused editor9/9 and target2/2 overlap those totals. No real OS clipboard content was read/written for manual acceptance, no real input-method candidate window was operated and no mail was sent. Thus native CJK candidate appearance, copy/cut/paste operation screenshots and complete mail acceptance remain open. Prepared4d6f973ac package predates this patch.
