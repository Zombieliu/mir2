# 已作废记录：选人界面对拍（原对象错误）

> **此报告作废（2026-08-18）。**
> 原稿对比的"原版选人界面"实为**创建角色页面**（角色模型 + CREATE），
> "Web 选人界面"实为**登录/过渡页面**——两者**不是同一个界面**，对拍对象错误，结论不可信。
> **正确的对拍见 `docs/UI-CONTRAST-LOGIN.zh-CN.md`（登录界面，已确认为同一界面）。**

## 错误复盘（为何作废）
1. 未先确认两张截图各自是**哪个界面**，凭脚本声称/文件命名判断界面身份。
2. 原版截图实为创建角色页，Web 截图实为登录页，硬凑成"一组"对比。
3. 结论（"结构对齐"/"视觉差异"）建立在错误对象上，无意义。

## 教训
- **对拍前必须先确认两边是同一界面**（多信号交叉：标题/DOM/像素/元素）。
- 用"结构上有这些元素"不代表"视觉对齐"；更要紧的是确认"比的是不是同一个界面"。

## 关联文件
- 正确登录对拍：`docs/UI-CONTRAST-LOGIN.zh-CN.md`
- 截图：`docs/ref/` 下
