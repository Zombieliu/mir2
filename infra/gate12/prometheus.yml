global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: obelisk-zone-host
    metrics_path: /metrics
    static_configs:
      - targets:
          - zone-host-a:9100
          - zone-host-b:9100

rule_files:
  - /etc/prometheus/rules/*.yml
